# docker-compose.yml
version: '3.8'
services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./local_repo:/app/local_repo
      - ./output:/app/output
      - ./logs:/app/logs
    env_file:
      - .env